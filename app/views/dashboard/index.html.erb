<!DOCTYPE html>
<html>
<head>
  <title>Pharma GPS Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root {
      --primary-blue: #0984C0;
      --accent-silver: #AAA7B0;
      --mid-grey: #565759;
      --bg-white: #FFFFFF;
      --light-variant: #C0BEC6;
      --lighter-blue: #60BDD1;
      --success-green: #10b981;
      --warning-yellow: #eab308;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, sans-serif;
      margin: 0; padding: 20px;
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      color: var(--mid-grey);
    }
    .header {
      background: linear-gradient(135deg, var(--primary-blue), var(--lighter-blue));
      color: var(--bg-white);
      padding: 2rem;
      border-radius: 16px;
      margin-bottom: 2rem;
      text-align: center;
      box-shadow: 0 10px 30px rgba(9,132,192,0.3);
    }
    .stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }
    .stat-card {
      background: var(--bg-white);
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(170,167,176,0.15);
      border-left: 5px solid var(--primary-blue);
      text-align: center;
    }
    .stat-number {
      font-size: 3rem;
      font-weight: bold;
      color: var(--primary-blue);
      margin-bottom: 0.5rem;
    }
    .vehicles {
      background: var(--bg-white);
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(170,167,176,0.15);
      border: 1px solid var(--accent-silver);
      max-height: 500px;
      overflow-y: auto;
      margin-bottom: 2rem;
    }
    .vehicles h2, .map-section h2 {
      color: var(--primary-blue);
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    .vehicle {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.5rem 0;
      border-bottom: 1px solid var(--light-variant);
    }
    .vehicle:last-child { border-bottom: none; }
    .vehicle-info h3 {
      margin: 0 0 0.5rem 0;
      color: var(--primary-blue);
      font-size: 1.2rem;
    }
    .coords {
      font-family: 'Courier New', monospace;
      font-size: 0.9rem;
      color: var(--mid-grey);
      margin: 0.25rem 0;
    }
    .status {
      font-weight: bold;
      font-size: 1.1rem;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      min-width: 80px;
      text-align: center;
    }
    .status-live { background: #dcfce7; color: var(--success-green); }
    .status-idle { background: #fef3c7; color: var(--warning-yellow); }
    .status-nodata { background: #f3f4f6; color: var(--accent-silver); }
    .driver-link {
      background: var(--primary-blue);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 8px;
      text-decoration: none;
      font-size: 0.9rem;
      font-weight: bold;
    }
    .driver-link:hover { background: var(--lighter-blue); }
    .map-section {
      background: var(--bg-white);
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(170,167,176,0.15);
      border: 1px solid var(--accent-silver);
    }
    #map {
      height: 500px;
      width: 100%;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(9,132,192,0.2);
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>🚚 Pharma Transport GPS</h1>
    <p style="opacity: 0.9; font-size: 1.2rem;">Phase 3C: LIVE | Real-time tracking + Google Maps</p>
  </div>

  <div class="stats">
    <div class="stat-card">
      <div class="stat-number"><%= Vehicle.count %></div>
      <div>Vehicles Active</div>
    </div>
    <div class="stat-card">
      <div class="stat-number"><%= Geofence.count %></div>
      <div>Geofences</div>
    </div>
    <div class="stat-card">
      <div class="stat-number">0</div>
      <div>Recent Alerts</div>
    </div>
    <div class="stat-card">
      <a href="/drivers/sign_up" class="driver-link">➕ Add Driver</a>
    </div>
  </div>

  <div class="vehicles">
    <h2>📍 Live Fleet Tracking</h2>
    <% @vehicles.each do |vehicle| %>
      <div class="vehicle">
        <div class="vehicle-info">
          <h3><%= vehicle.name %></h3>
          <div class="coords">Lat: <%= vehicle.latitude || 'N/A' %> | Lng: <%= vehicle.longitude || 'N/A' %></div>
          <% if vehicle.updated_at && vehicle.updated_at > 5.minutes.ago %>
            <span class="status status-live">🟢 LIVE</span>
          <% elsif vehicle.updated_at %>
            <span class="status status-idle">🟡 IDLE (<%= distance_of_time_in_words(vehicle.updated_at, Time.current) %> ago)</span>
          <% else %>
            <span class="status status-nodata">⚪ NO DATA</span>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="map-section">
    <h2>🗺️ Live Map View</h2>
    <div id="map"></div>
  </div>

  <script>
    function initMap() {
      const map = new google.maps.Map(document.getElementById("map"), {
        zoom: 11,
        center: {lat: 33.4484, lng: -112.0740},
        styles: [
          {featureType: "poi", elementType: "labels", stylers: [{visibility: "off"}]}
        ]
      });
      
      // Add truck markers
      <% @vehicles.each do |vehicle| %>
        <% if vehicle.latitude && vehicle.longitude %>
          new google.maps.Marker({
            position: {lat: <%= vehicle.latitude %>, lng: <%= vehicle.longitude %>},
            map: map,
            title: "<%= vehicle.name %> <%= vehicle.updated_at > 5.minutes.ago ? '🟢 LIVE' : '🟡 IDLE' %>",
            icon: {
              url: "data:image/svg+xml;charset=UTF-8," + encodeURIComponent(`
                <svg width="32" height="32" viewBox="0 0 32 32">
                  <rcle cx="16" cy="16" r="14" fill="<%= vehicle.updated_at > 5.minutes.ago ? '#10b981' : '#eab308' %>"/>
                  <text x="16" y="20" text-anchor="middle" fill="white" font-size="12" font-weight="bold"><%= vehicle.name[-1] %></text>
                </svg>
              `),
              scaledSize: new google.maps.Size(32, 32)
            }
          });
        <% end %>
      <% end %>
    }
  </script>
  <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBWM9oe_9Cvmh30pguQtq8n8TbM_orUiC8&callback=initMap"></script>

  <script>
    // Auto-refresh every 10 seconds
    setTimeout(() => location.reload(), 10000);
  </script>
</body>
</html>
<%= render "audit_trail" %>
<%= render "audit_trail" %>

<div class="card border-success mt-4 p-3">
  <div class="card-header bg-success text-white">
    <h5>✅ PHASE 6: 21 CFR Part 11 COMPLIANT</h5>
  </div>
  <div class="card-body">
    <div class="text-success fw-bold mb-2">✓ Audit logging ACTIVE</div>
    <div class="small text-success">
      • Immutable audit trail<br>
      • FDA electronic records<br>
      • Time-stamped events
    </div>
  </div>
</div>
